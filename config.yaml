region: eu-west-1

function_name: instance-manager
handler: service.handler
role: lambda_instance_manager
description: Function to manage the state of instances, start/stop/terminate/restart

# if access key and secret are left blank, boto will use the credentials
# defined in the [default] section of ~/.aws/credentials.
aws_access_key_id:
aws_secret_access_key:

# dist_directory: dist
# timeout: 15
# memory_size: 512
